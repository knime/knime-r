useDynLib(miscF)

export(curve.polynomial.rjmcmc, mvn.bayes, mvn.ub, mvt.ecme, mvt.mcmc,
       mvsn.mcmc, mvst.mcmc, rMultinom, rmvsn, rmvst, spatial.lme.mcmc, uvnm.rjmcmc)

import(Rcpp)
import(MASS)
import(R2jags)
import(BRugs)
import(MCMCpack)
import(mvtnorm)
importFrom("BayesBridge", "rtnorm")
importFrom("stats", "dbeta", "dnorm", "dpois", "lm", "median", "rbeta",
             "rchisq", "rgamma", "rnorm", "runif", "uniroot", "var")


